AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  This SAM template deploys resources associated with the backend ITM process for the (Analyse Bus Open Data) service

Globals:
  Function:
    Architectures:
      - x86_64
    Runtime: python3.11
    Timeout: 400
    MemorySize: 2048

Parameters:
  Environment:
    Type: String
    Description: The environment into which the stack is being deployed
  ProjectName:
    Description: The name of the project
    Default: 'abods'
    Type: String
  RdsDbHostAddr:
    Type: String
    Default: ''
  RdsDbPort:
    Type: Number
    Default: 5432
  VpcId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::VPC::Id>
    Default: ''
  VpcSubnets:
    Type: AWS::SSM::Parameter::Value<List<AWS::EC2::Subnet::Id>>
    Default: ''

Conditions:
  IsNotLocal: !Not [!Equals [!Ref Environment, 'local']]
  IsNotProd: !Not [!Equals [!Ref Environment, 'prod']]

Resources:
  ##########################################################
  #### SIRIVM S3 INGESTION FUNCTION (& ASSC. RESOURCES) ####
  ##########################################################
  SiriVmS3IngestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-s3-ingestion-function'
      ReservedConcurrentExecutions: 1
      CodeUri: ./sirivm_s3_ingestion_function
      Handler: app.lambda_handler
      KmsKeyArn: !If 
        - IsNotLocal
        - !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/lambda/arn}}'
        - !Ref AWS::NoValue
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref LandingBucket
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmIngestionQueue.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmProcessQueue.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmProcessQueue.Arn
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue1.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue2.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue3.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue4.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue5.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue6.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue7.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S1.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S2.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S3.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S4.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S5.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S6.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
              - 'sqs:SendMessage*'
            Resource: 
              - !GetAtt SiriVmOtpMatchingQueue.Arn
              - !GetAtt SiriVmOtpMatchingQueue1.Arn
              - !GetAtt SiriVmOtpMatchingQueue2.Arn
              - !GetAtt SiriVmOtpMatchingQueue3.Arn
              - !GetAtt SiriVmOtpMatchingQueue4.Arn
              - !GetAtt SiriVmOtpMatchingQueue5.Arn
              - !GetAtt SiriVmOtpMatchingQueue6.Arn
              - !GetAtt SiriVmOtpMatchingQueue7.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S1.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S2.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S3.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S4.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S5.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S6.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S7.Arn
              - !Sub 'arn:aws:sqs:${AWS::Region}:${AWS::AccountId}:${ProjectName}-${Environment}-sirivm-otp-queue-backfill*.fifo'
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmS3IngestionFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmIngestionQueue.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmS3IngestionFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
          SIRIVM_PROCESS_QUEUE: !If
            - IsNotLocal
            - !GetAtt SiriVmProcessQueue.QueueName
            - []
          SIRIVM_OTP_QUEUE: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue.QueueName
            - []
          SIRIVM_OTP_QUEUE1: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue1.QueueName
            - []
          SIRIVM_OTP_QUEUE2: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue2.QueueName
            - []
          SIRIVM_OTP_QUEUE3: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue3.QueueName
            - []
          SIRIVM_OTP_QUEUE4: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue4.QueueName
            - []
          SIRIVM_OTP_QUEUE5: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue5.QueueName
            - []
          SIRIVM_OTP_QUEUE6: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue6.QueueName
            - []
          SIRIVM_OTP_QUEUE7: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue7.QueueName
            - []
      LoggingConfig:
        LogGroup: !Ref SiriVmS3IngestionFunctionLogGroup

  SiriVmS3IngestionFunctionSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub 'Security group for ${ProjectName}-${Environment}-sirivm-s3-ingestion-function'
      GroupName: !Sub '${ProjectName}-${Environment}-sirivm-s3-ingestion-function'
      SecurityGroupEgress: 
        - IpProtocol: '-1'
          CidrIp: '0.0.0.0/0'
      VpcId: !Ref VpcId
      Tags:
        - Key: Name
          Value: !Sub '${ProjectName}-${Environment}-sirivm-s3-ingestion-function'

  SiriVmS3IngestionFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsNotLocal
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-sirivm-s3-ingestion-function'
      KmsKeyId: !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/cloudwatch/arn}}'
      RetentionInDays: 30

  ############################################################
  #### SIRIVM S3 INGESTION FUNCTION FOR HISTORIC MATCHING ####
  ############################################################
  SiriVmBackfillIngestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      Timeout: 900
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-backfill-ingestion-function'
      CodeUri: ./sirivm_s3_ingestion_function
      Handler: app.lambda_handler
      KmsKeyArn: !If 
        - IsNotLocal
        - !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/lambda/arn}}'
        - !Ref AWS::NoValue
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
              - 'sqs:SendMessage*'
            Resource: 
              - !Sub 'arn:aws:sqs:${AWS::Region}:${AWS::AccountId}:${ProjectName}-${Environment}-sirivm-otp-queue-backfill*.fifo'
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmS3IngestionFunctionLogGroup.Arn
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmS3IngestionFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
          SIRIVM_PROCESS_QUEUE: !If
            - IsNotLocal
            - !GetAtt SiriVmProcessQueue.QueueName
            - []
          SIRIVM_OTP_QUEUE: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue.QueueName
            - []
          SIRIVM_OTP_QUEUE1: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue1.QueueName
            - []
          SIRIVM_OTP_QUEUE2: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue2.QueueName
            - []
          SIRIVM_OTP_QUEUE3: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue3.QueueName
            - []
          SIRIVM_OTP_QUEUE4: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue4.QueueName
            - []
          SIRIVM_OTP_QUEUE5: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue5.QueueName
            - []
          SIRIVM_OTP_QUEUE6: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue6.QueueName
            - []
          SIRIVM_OTP_QUEUE7: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue7.QueueName
            - []
      LoggingConfig:
        LogGroup: !Ref SiriVmS3IngestionFunctionLogGroup

  ##########################################################
  #### SIRIVM DB INGESTION FUNCTION (& ASSC. RESOURCES) ####
  ##########################################################
  SiriVmDbIngestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-db-ingestion-function'
      CodeUri: ./sirivm_db_ingestion_function
      Handler: app.lambda_handler
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmProcessQueue.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmProcessQueue.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmDbIngestionFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmProcessQueue.Arn
            BatchSize: 1
            ScalingConfig:
              MaximumConcurrency: 2
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmDbIngestionFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
          POWERTOOLS_SERVICE_NAME: sirivm-db-ingestion-function
          POWERTOOLS_LOG_LEVEL: INFO
      LoggingConfig:
        LogGroup: !Ref SiriVmDbIngestionFunctionLogGroup

  SiriVmDbIngestionFunctionSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub 'Security group for ${ProjectName}-${Environment}-sirivm-db-ingestion-function'
      GroupName: !Sub '${ProjectName}-${Environment}-sirivm-db-ingestion-function'
      SecurityGroupEgress: 
        - IpProtocol: '-1'
          CidrIp: '0.0.0.0/0'
      VpcId: !Ref VpcId
      Tags:
        - Key: Name
          Value: !Sub '${ProjectName}-${Environment}-sirivm-db-ingestion-function'

  SiriVmDbIngestionFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsNotLocal
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-sirivm-db-ingestion-function'
      KmsKeyId: !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/cloudwatch/arn}}'
      RetentionInDays: 30

  #####################################################################
  #### SIRIVM TIMETABLE S3 GENERATION FUNCTION (& ASSC. RESOURCES) ####
  #####################################################################
  SiriVmTimetableS3GenerationFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-timetable-s3-generation-function'
      CodeUri: ./sirivm_timetable_s3_generation_function
      Handler: app.lambda_handler
      Timeout: 900
      MemorySize: 5000
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmTimetableS3GenerationFunctionLogGroup.Arn
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue1.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue2.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue3.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue4.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue5.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue6.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue7.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S1.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S2.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S3.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S4.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S5.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S6.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
              - 'sqs:SendMessage*'
            Resource: 
              - !GetAtt SiriVmOtpMatchingQueue.Arn
              - !GetAtt SiriVmOtpMatchingQueue1.Arn
              - !GetAtt SiriVmOtpMatchingQueue2.Arn
              - !GetAtt SiriVmOtpMatchingQueue3.Arn
              - !GetAtt SiriVmOtpMatchingQueue4.Arn
              - !GetAtt SiriVmOtpMatchingQueue5.Arn
              - !GetAtt SiriVmOtpMatchingQueue6.Arn
              - !GetAtt SiriVmOtpMatchingQueue7.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S1.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S2.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S3.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S4.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S5.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S6.Arn
              - !GetAtt SiriVmOtpBackfillQueue1S7.Arn
              - !Sub 'arn:aws:sqs:${AWS::Region}:${AWS::AccountId}:${ProjectName}-${Environment}-sirivm-otp-queue-backfill*.fifo'
      Events:
        EventName:
          Type: Schedule
          Properties:
            Schedule: cron(0/30 * * * ? *)      
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmTimetableS3GenerationFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
          SIRIVM_OTP_QUEUE: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue.QueueName
            - []
          SIRIVM_OTP_QUEUE1: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue1.QueueName
            - []
          SIRIVM_OTP_QUEUE2: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue2.QueueName
            - []
          SIRIVM_OTP_QUEUE3: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue3.QueueName
            - []
          SIRIVM_OTP_QUEUE4: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue4.QueueName
            - []
          SIRIVM_OTP_QUEUE5: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue5.QueueName
            - []
          SIRIVM_OTP_QUEUE6: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue6.QueueName
            - []
          SIRIVM_OTP_QUEUE7: !If
            - IsNotLocal
            - !GetAtt SiriVmOtpMatchingQueue7.QueueName
            - []
      LoggingConfig:
        LogGroup: !Ref SiriVmTimetableS3GenerationFunctionLogGroup

  SiriVmTimetableS3GenerationFunctionSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub 'Security group for ${ProjectName}-${Environment}-sirivm-timetable-s3-generation-function'
      GroupName: !Sub '${ProjectName}-${Environment}-sirivm-timetable-s3-generation-function'
      SecurityGroupEgress: 
        - IpProtocol: '-1'
          CidrIp: '0.0.0.0/0'
      VpcId: !Ref VpcId
      Tags:
        - Key: Name
          Value: !Sub '${ProjectName}-${Environment}-sirivm-timetable-s3-generation-function'

  SiriVmTimetableS3GenerationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsNotLocal
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-sirivm-timetable-s3-generation-function'
      KmsKeyId: !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/cloudwatch/arn}}'
      RetentionInDays: 30

  #############################################################
  #### TRAVELINE DB INGESTION FUNCTION (& ASSC. RESOURCES) ####
  #############################################################
  TravelineDbIngestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-traveline-db-ingestion-function'
      CodeUri: ./traveline_import_function
      Handler: app.lambda_handler
      Timeout: 60
      MemorySize: 512
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt TravelineDbIngestionFunctionLogGroup.Arn
      Events:
        TravelineSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(07 01 * * ? *)
            Name: !Sub 'TravelineSchedule${Environment}'
            Description: Traveline schedule
            Enabled: true
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt TravelineDbIngestionFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
      LoggingConfig:
        LogGroup: !Ref TravelineDbIngestionFunctionLogGroup

  TravelineDbIngestionFunctionSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub 'Security group for ${ProjectName}-${Environment}-traveline-db-ingestion-function'
      GroupName: !Sub '${ProjectName}-${Environment}-traveline-db-ingestion-function'
      SecurityGroupEgress: 
        - IpProtocol: '-1'
          CidrIp: '0.0.0.0/0'
      VpcId: !Ref VpcId
      Tags:
        - Key: Name
          Value: !Sub '${ProjectName}-${Environment}-traveline-db-ingestion-function'

  TravelineDbIngestionFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsNotLocal
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-traveline-db-ingestion-function'
      KmsKeyId: !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/cloudwatch/arn}}'
      RetentionInDays: 30

  ##########################################################
  #### SIRIVM OTP MATCHING FUNCTION (& ASSC. RESOURCES) ####
  ##########################################################
  SiriVmOtpMatchingFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue.Arn
            BatchSize: 1
            ScalingConfig:
              MaximumConcurrency: 4
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
          POWERTOOLS_SERVICE_NAME: sirivm-otp-matching-function
          POWERTOOLS_LOG_LEVEL: INFO
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunctionSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub 'Security group for ${ProjectName}-${Environment}-sirivm-otp-matching-function'
      GroupName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function'
      SecurityGroupEgress: 
        - IpProtocol: '-1'
          CidrIp: '0.0.0.0/0'
      VpcId: !Ref VpcId
      Tags:
        - Key: Name
          Value: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function'

  SiriVmOtpMatchingFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsNotLocal
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-sirivm-otp-matching-function'
      KmsKeyId: !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/cloudwatch/arn}}'
      RetentionInDays: 30

  #########################################
  #### GENERAL CONFIGURATION RESOURCES ####
  #########################################
  IngestionFunctionLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: !Sub '${ProjectName}-${Environment}-ingestion-layer'
      ContentUri: ./ingestion_layer
      CompatibleRuntimes:
        - python3.11
    Metadata:
      BuildMethod: python3.11

  SnsSubscriptionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sns-subscription-function'
      CodeUri: ./sns_subscription_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 90
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - 'sqs:ReceiveMessage'
                - 'sqs:DeleteMessage'
                - 'sqs:GetQueueAttributes'
              Resource: !GetAtt SiriVmIngestionQueue.Arn
        - Statement:
            - Effect: Allow
              Action:
                - 'sns:Subscribe'
                - 'sns:Unsubscribe'
                - 'sns:ConfirmSubscription'
                - 'sns:ListSubscriptionsByTopic'
              Resource: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/sns/avl-topic/arn}}'

  SnsSubscriptionCustomResource:
    Type: Custom::SNSSubscription
    Properties:
      ServiceToken: !GetAtt SnsSubscriptionFunction.Arn
      SnsTopicArn: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/sns/avl-topic/arn}}'
      SqsQueueArn: !GetAtt SiriVmIngestionQueue.Arn
      SqsQueueUrl: !GetAtt SiriVmIngestionQueue.QueueUrl


  LandingBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    Condition: IsNotLocal
    Properties:
      BucketName: !Sub '${ProjectName}-${Environment}-landing-bucket'
      NotificationConfiguration:
        QueueConfigurations:
          - Event: s3:ObjectCreated:*
            Queue: !GetAtt SiriVmIngestionQueue.Arn

  ProcessBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    Condition: IsNotLocal
    Properties:
      BucketName: !Sub '${ProjectName}-${Environment}-process-bucket'

  SiriVmIngestionQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-ingestion-queue'
      VisibilityTimeout: 430

  SiriVmIngestionQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref SiriVmIngestionQueue
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: s3.amazonaws.com
            Action: sqs:SendMessage
            Resource: !GetAtt SiriVmIngestionQueue.Arn
            Condition:
              ArnLike:
                aws:SourceArn: !Sub 'arn:aws:s3:::${ProjectName}-${Environment}-landing-bucket'
          - Effect: Allow
            Principal:
              Service: s3.amazonaws.com
            Action: sqs:SendMessage
            Resource: !GetAtt SiriVmIngestionQueue.Arn
            Condition:
              ArnLike:
                aws:SourceArn: !Sub 'arn:aws:s3:::{{resolve:ssm:/${ProjectName}/${Environment}/s3/sirivm}}'
          - Effect: Allow
            Principal:
              Service: sns.amazonaws.com
            Action: sqs:SendMessage
            Resource: !GetAtt SiriVmIngestionQueue.Arn
            Condition:
              ArnLike:
                aws:SourceArn: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/sns/avl-topic/arn}}'

  SiriVmProcessQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-process-queue'
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue'
      VisibilityTimeout: 430

  ####################################################################
  #### SIRIVM OTP PARALLEL MATCHING FUNCTIONS (& ASSC. RESOURCES) ####
  ####################################################################

  SiriVmOtpMatchingQueue1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingQueue7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430

  SiriVmOtpMatchingFunction1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunction2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunction3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunction4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunction5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunction6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  SiriVmOtpMatchingFunction7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-matching-function7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpMatchingQueue7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpMatchingQueue7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpMatchingFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpMatchingQueue7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpMatchingFunctionLogGroup

  #############################################################################
  #### SIRIVM OTP PARALLEL BACKFILL MATCHING FUNCTIONS (& ASSC. RESOURCES) ####
  #############################################################################

  SiriVmOtpBackfillFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsNotLocal
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-sirivm-otp-backfill-function'
      KmsKeyId: !Sub '{{resolve:ssm:/${ProjectName}/shared/kms/cloudwatch/arn}}'
      RetentionInDays: 30

  SiriVmOtpBackfillQueue1S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue1S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue1S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue1S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue1S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue1S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue1S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-1-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue2S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-2-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue3S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-3-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue4S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-4-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue5S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-5-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue6S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-6-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue7S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-7-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue8S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-8-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue9S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-9-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-1.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S2:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-2.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S3:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-3.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S4:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-4.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S5:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-5.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S6:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-6.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillQueue10S7:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub '${ProjectName}-${Environment}-sirivm-otp-queue-backfill-10-7.fifo'
      FifoQueue: true
      ContentBasedDeduplication: false
      VisibilityTimeout: 430
        
  SiriVmOtpBackfillFunction1S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction1S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction1S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction1S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction1S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction1S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction1S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-1-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue1S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue1S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue1S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction2S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-2-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue2S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue2S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue2S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction3S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-3-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue3S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue3S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue3S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction4S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-4-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue4S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue4S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue4S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction5S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-5-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue5S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue5S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue5S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction6S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-6-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue6S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue6S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue6S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction7S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-7-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue7S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue7S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue7S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction8S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-8-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue8S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue8S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue8S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction9S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-9-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue9S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue9S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue9S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S1:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-1'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S1.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S1.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S1.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S2:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-2'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S2.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S2.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S2.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-3'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S3.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S3.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S3.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S4:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-4'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S4.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S4.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S4.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S5:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-5'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S5.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S5.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S5.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S6:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-6'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S6.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S6.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S6.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        
  SiriVmOtpBackfillFunction10S7:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-sirivm-otp-backfill-function-10-7'
      CodeUri: ./sirivm_otp_matching_function
      Handler: sirivm_otp_matching_function.app.lambda_handler
      Timeout: 60
      MemorySize: 5000
      ReservedConcurrentExecutions: 1
      Layers: !If
        - IsNotLocal
        - - !Ref IngestionFunctionLayer
          - "arn:aws:lambda:eu-west-2:336392948345:layer:AWSSDKPandas-Python311:12"
        - []
      Policies:
        - 'AWSLambdaVPCAccessExecutionRole'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/rds_proxy_rw_policy/arn}}'
        - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/iam/sirivm_access_policy/arn}}'
        - S3CrudPolicy:
            BucketName: !Ref ProcessBucket
        - SQSPollerPolicy:
            QueueName: !GetAtt SiriVmOtpBackfillQueue10S7.QueueName
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - 'sqs:GetQueueUrl'
            Resource: !GetAtt SiriVmOtpBackfillQueue10S7.Arn
        - Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: !GetAtt SiriVmOtpBackfillFunctionLogGroup.Arn
      Events:
        SQSTemplateTriggerEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt SiriVmOtpBackfillQueue10S7.Arn
            BatchSize: 1
      VpcConfig:
        SubnetIds: !Ref VpcSubnets
        SecurityGroupIds:
          - !GetAtt SiriVmOtpMatchingFunctionSecurityGroup.GroupId
          - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/ec2/securitygroup/rdsproxy-access/id}}'
      Environment:
        Variables:
          PROJECT_ENV: !Ref Environment
          PROJECT_NAME: !Ref ProjectName
          POSTGRES_HOST: !If
            - IsNotLocal
            - !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rdsproxy/endpoint}}'
            - !Ref RdsDbHostAddr
          POSTGRES_PORT: !Ref RdsDbPort
          POSTGRES_DB: !Sub '{{resolve:ssm:/${ProjectName}/${Environment}/rds/dbname}}'
          POSTGRES_USER: !Sub '${ProjectName}_proxy_rw'
          SIRIVM_BUCKET: !If
            - IsNotLocal
            - !Ref ProcessBucket
            - !Ref AWS::NoValue
      LoggingConfig:
        LogGroup: !Ref SiriVmOtpBackfillFunctionLogGroup
        


